{
  "LLMSettings": {
    "ApiKey": "", // Do not store the actual API key here
    "ModelName": "gemini-pro",
    "Temperature": 0.7,
    "MaxTokens": 2048
  },
  "Logging": {
    "SeqServerUrl": "http://localhost:5341"
  }
}

//to configure the API key, use the command:
//dotnet user-secrets set "LLMSettings:ApiKey" "your-actual-api-key" --project AIClient
